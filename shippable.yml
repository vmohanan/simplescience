# Language setting
language: java

# Version number
jdk:
  - oraclejdk8

build:
  ci:
    - java -version
    - ls -l
    
before_script: 
  - mkdir -p shippable/testresults
  - mkdir -p shippable/codecoverage

script:
  - mvn clean install cobertura:cobertura 
