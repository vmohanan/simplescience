# Language setting
language: java

# Version number
jdk:
  - oraclejdk8

build:
  ci:
    - java -version
    - ls -l
    - ls -l target
    
before_script: 
  - mkdir -p shippable/testresults
  - mkdir -p shippable/codecoverage

script:
  - mvn install cobertura:cobertura 

after_script:
  - ls -l
  - ls -l target
